{
  "tools": [
    {
      "name": "pr_view",
      "description": "View pull request details including title, body, state, author, labels, reviewers, and file change stats. Use for understanding PR context, checking review decisions, or getting PR description content. Pass 'fields' for efficient JSON selection (e.g. 'title,body,state,reviews'). Requires the PR 'number'. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Defaults to the repo configured in .mcp-gh-tooling.json."
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields to return (e.g. 'title,body,state,reviews,comments,files'). Returns formatted text output when omitted."
          },
          "comments": {
            "type": "boolean",
            "description": "Include PR comments in output. Only applies when 'fields' is not set.",
            "default": false
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful for long PR descriptions.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_diff",
      "description": "Get the unified diff (code changes) for a pull request. Use to inspect actual code modifications. Optionally filter to a single file path with 'file'. Use 'name_only' to list only changed file names without the diff content. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "file": {
            "type": "string",
            "description": "Limit diff to this specific file path (e.g. 'src/Core/Content/Product/ProductEntity.php')."
          },
          "name_only": {
            "type": "boolean",
            "description": "List only changed file names, not the diff content.",
            "default": false
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful for large diffs.",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output. Applied after max_lines if both are set.",
            "minimum": 1
          },
          "grep_pattern": {
            "type": "string",
            "description": "Filter output lines matching this extended regex (grep -E). Non-matching lines are omitted. Exit 1 (no matches) is treated as success with empty output."
          },
          "grep_context_before": {
            "type": "integer",
            "description": "Lines of context to include before each grep match (-B).",
            "default": 0,
            "minimum": 0
          },
          "grep_context_after": {
            "type": "integer",
            "description": "Lines of context to include after each grep match (-A).",
            "default": 0,
            "minimum": 0
          },
          "grep_ignore_case": {
            "type": "boolean",
            "description": "Case-insensitive grep matching (-i).",
            "default": false
          },
          "grep_invert": {
            "type": "boolean",
            "description": "Return lines that do NOT match grep_pattern (-v).",
            "default": false
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_list",
      "description": "List pull requests with optional filters. Use to find PRs by author, state, search expression, or branch. Supports JSON output for structured data. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "author": {
            "type": "string",
            "description": "Filter by PR author (GitHub username)."
          },
          "state": {
            "type": "string",
            "enum": [
              "open",
              "closed",
              "merged",
              "all"
            ],
            "description": "Filter by PR state."
          },
          "search": {
            "type": "string",
            "description": "Search expression (e.g. 'NEXT-1234', 'trailing slashes', 'is:draft')."
          },
          "head": {
            "type": "string",
            "description": "Filter by source branch name."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results to return.",
            "default": 20
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'number,title,state,url,mergedAt')."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_checks",
      "description": "View the CI/CD status checks for a pull request. Shows check names, status (queued/in_progress/completed), and conclusions (success/failure/skipped). Use to quickly assess if a PR's CI is passing. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful when there are many CI checks.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_comments",
      "description": "Get inline review comments (code-level comments on specific lines) for a pull request. These are reviewer annotations on the diff, not general conversation comments. Use to read detailed reviewer feedback on specific code lines. Returns paginated JSON by default. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "paginate": {
            "type": "boolean",
            "description": "Fetch all pages of results.",
            "default": true
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform output (e.g. '.[] | {path, body, line, user: .user.login}'). Syntax is validated before execution."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful when there are many comment threads.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_reviews",
      "description": "Get the reviews for a pull request. Returns reviewer decisions (APPROVED, CHANGES_REQUESTED, COMMENTED, DISMISSED) and review body text. Use to understand the review history and current approval status. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform output (e.g. '.[] | select(.state == \"CHANGES_REQUESTED\") | {user: .user.login, body}'). Syntax is validated before execution."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful when there are many reviews.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_files",
      "description": "Get the list of files changed in a pull request with patch content. Default output shows filename, status, additions, and deletions. Use 'jq_filter' to extract patch content for specific files (e.g. filter by filename to get the actual code diff). Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter files. Default: '.[] | {filename, status, additions, deletions}'. Example: '.[] | select(.filename | contains(\"Migration\")) | {filename, patch}'. Syntax is validated before execution."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "pr_commits",
      "description": "Get the commit history for a pull request. Returns commit SHAs and first-line commit messages by default. Use to understand what changes were made across commits in a PR. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Pull request number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to transform output. Default: '.[] | {sha: .sha[0:10], message: (.commit.message | split(\"\\n\")[0])}'. Syntax is validated before execution."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "issue_view",
      "description": "View a GitHub issue including title, body, state, labels, assignees, and milestone. Use to understand bug reports, feature requests, or task descriptions. Pass 'with_comments' to include discussion thread. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": [
              "integer",
              "string"
            ],
            "description": "Issue number."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'title,body,state,labels,comments')."
          },
          "with_comments": {
            "type": "boolean",
            "description": "Include issue comments in text output. Only applies when 'fields' is not set.",
            "default": false
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful for long issue bodies.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "issue_list",
      "description": "List GitHub issues with optional filters. Use to find issues by search expression, state, or label. Repository: pass `repo`, `repository`, or `owner`+`repo`.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "search": {
            "type": "string",
            "description": "Search expression (e.g. 'TODO label:component/core', 'custom field translation')."
          },
          "state": {
            "type": "string",
            "enum": [
              "open",
              "closed",
              "all"
            ],
            "description": "Filter by issue state."
          },
          "label": {
            "type": "string",
            "description": "Filter by label name."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 20
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'number,title,state,labels,createdAt')."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner. Used with `repo` (the bare repository name) as a split form. Alternative to `repository`."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in `owner/repo` format. Alias of `repo`; choose whichever shape matches your data."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "run_view",
      "description": "View the status and summary of a GitHub Actions workflow run. Returns run state (queued/in_progress/completed), conclusion (success/failure/cancelled), and job list. Use to check if CI passed and identify which jobs failed.",
      "inputSchema": {
        "type": "object",
        "required": [
          "run_id"
        ],
        "properties": {
          "run_id": {
            "type": [
              "integer",
              "string"
            ],
            "description": "GitHub Actions workflow run ID (numeric, e.g. 21534190745)."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'status,conclusion,jobs')."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "run_list",
      "description": "List recent GitHub Actions workflow runs. Use to find runs for a specific branch, workflow, status, or user. Supports filtering by event type, creation date, and commit SHA.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "branch": {
            "type": "string",
            "description": "Filter runs by branch name."
          },
          "workflow": {
            "type": "string",
            "description": "Filter runs by workflow name or filename (e.g. 'CI', 'build.yml')."
          },
          "status": {
            "type": "string",
            "description": "Filter runs by status.",
            "enum": [
              "queued",
              "in_progress",
              "completed",
              "waiting",
              "requested",
              "pending",
              "action_required",
              "cancelled",
              "failure",
              "neutral",
              "skipped",
              "stale",
              "startup_failure",
              "success",
              "timed_out"
            ]
          },
          "event": {
            "type": "string",
            "description": "Filter runs by trigger event (e.g. 'push', 'pull_request', 'schedule', 'workflow_dispatch')."
          },
          "user": {
            "type": "string",
            "description": "Filter runs by GitHub username who triggered the workflow."
          },
          "created": {
            "type": "string",
            "description": "Filter runs by creation date range (e.g. '>2024-01-01', '2024-01-01..2024-06-30')."
          },
          "commit": {
            "type": "string",
            "description": "Filter runs by commit SHA."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 20
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'databaseId,status,conclusion,headBranch,workflowName')."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "run_logs",
      "description": "Get logs for a GitHub Actions workflow run. Returns failed step logs by default (recommended for CI debugging). Set 'failed_only' to false to get all step logs. Use 'max_lines'/'tail_lines' to limit output, or 'grep_pattern' to filter for specific error messages.",
      "inputSchema": {
        "type": "object",
        "required": [
          "run_id"
        ],
        "properties": {
          "run_id": {
            "type": [
              "integer",
              "string"
            ],
            "description": "GitHub Actions workflow run ID."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "failed_only": {
            "type": "boolean",
            "description": "Return only failed step logs (recommended). Set to false to get all step logs.",
            "default": true
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Applied before tail_lines if both are set.",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output. Applied after max_lines if both are set.",
            "minimum": 1
          },
          "grep_pattern": {
            "type": "string",
            "description": "Filter output lines matching this extended regex (grep -E). Non-matching lines are omitted. Exit 1 (no matches) is treated as success with empty output."
          },
          "grep_context_before": {
            "type": "integer",
            "description": "Lines of context to include before each grep match (-B).",
            "default": 0,
            "minimum": 0
          },
          "grep_context_after": {
            "type": "integer",
            "description": "Lines of context to include after each grep match (-A).",
            "default": 0,
            "minimum": 0
          },
          "grep_ignore_case": {
            "type": "boolean",
            "description": "Case-insensitive grep matching (-i).",
            "default": false
          },
          "grep_invert": {
            "type": "boolean",
            "description": "Return lines that do NOT match grep_pattern (-v).",
            "default": false
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "workflow_jobs",
      "description": "Aggregate jobs across workflow runs in a single call. Reduces N+1 tool calls (run_list + N×job_view) to one invocation. Fetches runs for a workflow, then retrieves jobs for each run. Filter by job name, conclusion, and step name. Steps are excluded by default — only included when 'step' filter is set.",
      "inputSchema": {
        "type": "object",
        "required": [
          "workflow"
        ],
        "properties": {
          "workflow": {
            "type": "string",
            "description": "Workflow name or filename (e.g. 'CI', 'build.yml'). Required."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required (pass explicitly or configure default in .mcp-gh-tooling.json)."
          },
          "job": {
            "type": "string",
            "description": "Filter jobs by name (case-insensitive substring match). E.g. 'PHPStan', 'unit test'."
          },
          "conclusion": {
            "type": "string",
            "description": "Filter jobs by conclusion (e.g. 'failure', 'success', 'cancelled')."
          },
          "step": {
            "type": "string",
            "description": "Filter and include steps by name (case-insensitive substring). Steps are excluded from output unless this param is set."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of workflow runs to fetch (each run = 1 API call). Default: 5.",
            "default": 5,
            "minimum": 1,
            "maximum": 50
          },
          "run_status": {
            "type": "string",
            "description": "Filter runs by status (e.g. 'completed', 'failure', 'success').",
            "enum": [
              "queued",
              "in_progress",
              "completed",
              "waiting",
              "requested",
              "pending",
              "action_required",
              "cancelled",
              "failure",
              "neutral",
              "skipped",
              "stale",
              "startup_failure",
              "success",
              "timed_out"
            ]
          },
          "branch": {
            "type": "string",
            "description": "Filter runs by branch name."
          },
          "event": {
            "type": "string",
            "description": "Filter runs by trigger event (e.g. 'push', 'pull_request')."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the final output. Syntax is validated before execution."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N).",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "job_view",
      "description": "Get details for a specific GitHub Actions job by job ID. Returns job status, conclusion, runner info, and individual step statuses with timing. Use after run_view to inspect a specific failing job.",
      "inputSchema": {
        "type": "object",
        "required": [
          "job_id"
        ],
        "properties": {
          "job_id": {
            "type": [
              "integer",
              "string"
            ],
            "description": "GitHub Actions job ID (numeric, e.g. 62056364818)."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter output (e.g. '.steps[] | select(.conclusion == \"failure\") | {name, conclusion, number}'). Syntax is validated before execution."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "job_logs",
      "description": "Get the raw log output for a specific GitHub Actions job by job ID. More targeted than run_logs - use when you know the specific job that failed. Use 'max_lines'/'tail_lines' to limit output, or 'grep_pattern' to filter for specific error messages.",
      "inputSchema": {
        "type": "object",
        "required": [
          "job_id"
        ],
        "properties": {
          "job_id": {
            "type": [
              "integer",
              "string"
            ],
            "description": "GitHub Actions job ID (numeric, e.g. 62056364818)."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Applied before tail_lines if both are set.",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output. Applied after max_lines if both are set.",
            "minimum": 1
          },
          "grep_pattern": {
            "type": "string",
            "description": "Filter output lines matching this extended regex (grep -E). Non-matching lines are omitted. Exit 1 (no matches) is treated as success with empty output."
          },
          "grep_context_before": {
            "type": "integer",
            "description": "Lines of context to include before each grep match (-B).",
            "default": 0,
            "minimum": 0
          },
          "grep_context_after": {
            "type": "integer",
            "description": "Lines of context to include after each grep match (-A).",
            "default": 0,
            "minimum": 0
          },
          "grep_ignore_case": {
            "type": "boolean",
            "description": "Case-insensitive grep matching (-i).",
            "default": false
          },
          "grep_invert": {
            "type": "boolean",
            "description": "Return lines that do NOT match grep_pattern (-v).",
            "default": false
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "job_annotations",
      "description": "Get check annotations (inline error/warning messages pinned to code lines) for a CI check run. Useful for getting PHPStan errors, ESLint violations, or test failure details directly from GitHub's annotation system.",
      "inputSchema": {
        "type": "object",
        "required": [
          "check_run_id"
        ],
        "properties": {
          "check_run_id": {
            "type": [
              "integer",
              "string"
            ],
            "description": "GitHub check run ID (same as job ID for Actions jobs, e.g. 62056364818)."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter annotations (e.g. '.[] | {path, start_line, message, annotation_level}'). Syntax is validated before execution."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "commit_pulls",
      "description": "List GitHub pull requests associated with a pushed commit SHA. GitHub-only — no local git equivalent. Use when you need to know which PR introduced a specific change. The commit must be pushed and visible on GitHub. For local commit metadata (files changed, commit message) use 'git show <sha>' via Bash.",
      "inputSchema": {
        "type": "object",
        "required": [
          "sha"
        ],
        "properties": {
          "sha": {
            "type": "string",
            "description": "Commit SHA (7-40 hex characters). Must be pushed to GitHub."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Required if no default repo configured."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the PR list. Syntax is validated before execution."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search",
      "description": "Search for GitHub issues or pull requests using a search expression. Use to find related work, historical context, or PRs that fixed similar problems. Set 'type' to 'issues' for issues or 'prs' for pull requests.",
      "inputSchema": {
        "type": "object",
        "required": [
          "search"
        ],
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "issues",
              "prs"
            ],
            "description": "Search for issues or pull requests.",
            "default": "prs"
          },
          "repo": {
            "type": "string",
            "description": "Limit search to this repository in 'owner/repo' format."
          },
          "state": {
            "type": "string",
            "enum": [
              "open",
              "closed"
            ],
            "description": "Filter by open or closed state."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 20
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'number,title,state,url,createdAt')."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution. Useful for shaping JSON responses or reducing response size before the token cap."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          },
          "search": {
            "type": "string",
            "description": "Search expression (e.g. 'NEXT-3412', 'attribute entity', 'custom field translated')."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_code",
      "description": "Search for code across GitHub repositories using the legacy code search engine. Finds files containing specific text. Rate limit: 10 requests/minute. No regex, symbol search, or path glob support — use exact text matches. Set 'download_to' to save matching files locally instead of returning search results.",
      "inputSchema": {
        "type": "object",
        "required": [
          "search"
        ],
        "properties": {
          "owner": {
            "type": "string",
            "description": "Limit search to repositories owned by this user/organization."
          },
          "repo": {
            "type": "string",
            "description": "Limit search to this repository in 'owner/repo' format."
          },
          "language": {
            "type": "string",
            "description": "Filter by programming language (e.g. 'php', 'typescript', 'javascript')."
          },
          "extension": {
            "type": "string",
            "description": "Filter by file extension (e.g. 'php', 'ts', 'json')."
          },
          "filename": {
            "type": "string",
            "description": "Filter by filename (e.g. 'composer.json', 'package.json')."
          },
          "match": {
            "type": "string",
            "enum": [
              "file",
              "path"
            ],
            "description": "Restrict matches to file contents ('file') or file path ('path')."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 30
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields (e.g. 'repository,path,textMatch'). Default: 'repository,path,textMatch'."
          },
          "download_to": {
            "type": "string",
            "description": "Local directory path. When set, downloads all matching files to {download_to}/{owner}/{repo}/{path} and returns a summary instead of search results."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution."
          },
          "grep_pattern": {
            "type": "string",
            "description": "Filter output lines matching this extended regex (grep -E)."
          },
          "grep_context_before": {
            "type": "integer",
            "description": "Lines of context before each grep match (-B).",
            "default": 0,
            "minimum": 0
          },
          "grep_context_after": {
            "type": "integer",
            "description": "Lines of context after each grep match (-A).",
            "default": 0,
            "minimum": 0
          },
          "grep_ignore_case": {
            "type": "boolean",
            "description": "Case-insensitive grep matching (-i).",
            "default": false
          },
          "grep_invert": {
            "type": "boolean",
            "description": "Return lines that do NOT match grep_pattern (-v).",
            "default": false
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N).",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          },
          "search": {
            "type": "string",
            "description": "Code search expression (exact text match, no regex). Example: 'addClass', 'extends AbstractController'."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_repos",
      "description": "Search for GitHub repositories by search expression, owner, topic, language, license, or star count. `search` is optional — filters alone suffice (e.g. search by owner and language without a search expression).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "owner": {
            "type": "string",
            "description": "Filter by repository owner (user or organization)."
          },
          "topic": {
            "type": "string",
            "description": "Filter by repository topic tag."
          },
          "language": {
            "type": "string",
            "description": "Filter by primary language (e.g. 'php', 'typescript')."
          },
          "license": {
            "type": "string",
            "description": "Filter by license (SPDX identifier, e.g. 'mit', 'apache-2.0')."
          },
          "stars": {
            "type": "string",
            "description": "Filter by star count range (e.g. '>100', '50..200', '>=1000')."
          },
          "sort": {
            "type": "string",
            "enum": [
              "stars",
              "forks",
              "help-wanted-issues",
              "updated"
            ],
            "description": "Sort order for results."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 20
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields. Default: 'fullName,description,stargazersCount,language,updatedAt,url'."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N).",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          },
          "search": {
            "type": "string",
            "description": "Search expression (optional when using filters). Example: 'shopware plugin', 'ecommerce'."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_commits",
      "description": "Search for GitHub commits across repositories by message text, author, date range, or hash. Use to find commits referencing ticket numbers, specific authors, or date ranges.",
      "inputSchema": {
        "type": "object",
        "required": [
          "search"
        ],
        "properties": {
          "repo": {
            "type": "string",
            "description": "Limit search to this repository in 'owner/repo' format."
          },
          "owner": {
            "type": "string",
            "description": "Limit search to repositories owned by this user/organization."
          },
          "author": {
            "type": "string",
            "description": "Filter by commit author username."
          },
          "committer": {
            "type": "string",
            "description": "Filter by committer username."
          },
          "author_date": {
            "type": "string",
            "description": "Filter by author date range (e.g. '>2024-01-01', '2024-01-01..2024-06-30')."
          },
          "committer_date": {
            "type": "string",
            "description": "Filter by committer date range."
          },
          "hash": {
            "type": "string",
            "description": "Filter by commit SHA prefix."
          },
          "merge": {
            "type": "boolean",
            "description": "Filter to merge commits only (true) or exclude merge commits (false)."
          },
          "sort": {
            "type": "string",
            "enum": [
              "author-date",
              "committer-date"
            ],
            "description": "Sort order for results."
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 20
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated JSON fields. Default: 'sha,commit'."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Syntax is validated before execution."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          },
          "search": {
            "type": "string",
            "description": "Commit message search text (e.g. 'NEXT-1234', 'fix cart calculation')."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "search_discussions",
      "description": "Search for GitHub discussions using GraphQL. Discussions are only available via GraphQL (no CLI subcommand). Use to find RFCs, Q&A threads, or community discussions. Set 'with_comments' to include discussion comment bodies.",
      "inputSchema": {
        "type": "object",
        "required": [
          "search"
        ],
        "properties": {
          "repo": {
            "type": "string",
            "description": "Limit search to this repository in 'owner/repo' format."
          },
          "category": {
            "type": "string",
            "description": "Filter by discussion category name (e.g. 'RFC', 'Q&A', 'Ideas')."
          },
          "author": {
            "type": "string",
            "description": "Filter by discussion author username."
          },
          "state": {
            "type": "string",
            "description": "State qualifier (e.g. 'is:answered', 'is:unanswered', 'is:open', 'is:closed')."
          },
          "with_comments": {
            "type": "boolean",
            "description": "Include full comment bodies and replies. When false (default), only returns comment count.",
            "default": false
          },
          "limit": {
            "type": "integer",
            "description": "Maximum number of results.",
            "default": 20
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression applied to the GraphQL response. Default extracts '.data.search.nodes'. Custom filters replace the default (operate on the full GraphQL response)."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N).",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          },
          "search": {
            "type": "string",
            "description": "Discussion search expression (e.g. 'RFC', 'authentication flow', 'plugin system')."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "repo_tree",
      "description": "Browse repository directory contents or get the full recursive file tree. Accepts GitHub URLs (e.g. 'https://github.com/owner/repo/tree/main/src'), explicit owner/repo params, or a repository string. Use instead of WebFetch on GitHub tree URLs. Set 'recursive' to true for the full repository tree. URL limitation: refs with slashes (e.g. feature/my-branch) are not parsed correctly — use explicit 'ref' param instead.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "owner": {
            "type": "string",
            "description": "Repository owner (user or organization). Used with 'repo'."
          },
          "repo": {
            "type": "string",
            "description": "Repository name. Used with 'owner'."
          },
          "repository": {
            "type": "string",
            "description": "Repository in 'owner/repo' format. Alternative to separate 'owner' and 'repo' params."
          },
          "path": {
            "type": "string",
            "description": "Directory path within the repository. Default: root.",
            "default": ""
          },
          "ref": {
            "type": "string",
            "description": "Git ref (branch, tag, or SHA). Default: repo default branch."
          },
          "recursive": {
            "type": "boolean",
            "description": "Get the full recursive file tree via the Git Trees API instead of a single directory listing.",
            "default": false
          },
          "url": {
            "type": "string",
            "description": "GitHub URL to parse (e.g. 'https://github.com/owner/repo/tree/main/src'). Owner, repo, ref, and path are extracted from the URL. Explicit params override URL-extracted values."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the output. Default: '[.[] | {name, type, size, path}]' (non-recursive) or '[.tree[] | {path, type, size}]' (recursive). Custom filters replace the default."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "repo_file",
      "description": "Fetch a single file from a GitHub repository as raw text. Accepts GitHub URLs (e.g. 'https://github.com/owner/repo/blob/main/README.md'), explicit owner/repo params, or a repository string. Use instead of WebFetch on GitHub blob URLs. Supports line ranges, grep filtering, and local download via 'download_to'. URL limitation: refs with slashes (e.g. feature/my-branch) are not parsed correctly — use explicit 'ref' param instead.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "owner": {
            "type": "string",
            "description": "Repository owner (user or organization). Used with 'repo'."
          },
          "repo": {
            "type": "string",
            "description": "Repository name. Used with 'owner'."
          },
          "repository": {
            "type": "string",
            "description": "Repository in 'owner/repo' format. Alternative to separate 'owner' and 'repo' params."
          },
          "path": {
            "type": "string",
            "description": "File path within the repository. Required unless provided via URL."
          },
          "ref": {
            "type": "string",
            "description": "Git ref (branch, tag, or SHA). Default: repo default branch."
          },
          "url": {
            "type": "string",
            "description": "GitHub URL to parse (e.g. 'https://github.com/owner/repo/blob/main/README.md'). Owner, repo, ref, and path are extracted from the URL. Explicit params override URL-extracted values."
          },
          "line_start": {
            "type": "integer",
            "description": "First line to return (1-indexed). Used with line_end for line range extraction.",
            "minimum": 1
          },
          "line_end": {
            "type": "integer",
            "description": "Last line to return (inclusive). Used with line_start for line range extraction.",
            "minimum": 1
          },
          "download_to": {
            "type": "string",
            "description": "Local file path. When set, saves the raw file content to this path and returns a confirmation message."
          },
          "grep_pattern": {
            "type": "string",
            "description": "Filter output lines matching this extended regex (grep -E)."
          },
          "grep_context_before": {
            "type": "integer",
            "description": "Lines of context before each grep match (-B).",
            "default": 0,
            "minimum": 0
          },
          "grep_context_after": {
            "type": "integer",
            "description": "Lines of context after each grep match (-A).",
            "default": 0,
            "minimum": 0
          },
          "grep_ignore_case": {
            "type": "boolean",
            "description": "Case-insensitive grep matching (-i).",
            "default": false
          },
          "grep_invert": {
            "type": "boolean",
            "description": "Return lines that do NOT match grep_pattern (-v).",
            "default": false
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N).",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "release_list",
      "description": "Look up GitHub release versions for one or more repositories. Built for dependency-update tasks: instead of calling api_read once per repo for repos/{owner}/{repo}/releases/latest, pass a 'repos' array and get the latest version of each in one call. Goes beyond 'latest' — filter to a major or major.minor line with 'constraint' (e.g. '4' or '4.2'), include prereleases/drafts, list multiple releases, and resolve each tag to its commit SHA for pinning GitHub Actions. Releases are sorted by semantic version (descending), not by publish date, so the highest version is treated as latest. Use 'fields' to return only the data you need (e.g. ['tag_name'] for just version numbers) or 'jq_filter' to reduce the result to a single field (e.g. '.[].tag_name').",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repos": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Batch mode: a list of repositories in 'owner/repo' format. When provided, the single-repo parameters (repo/owner/repository/url) are ignored and one entry is returned per repo."
          },
          "repo": {
            "type": "string",
            "description": "Single repository in 'owner/repo' format. Required if no default repo configured and 'repos' is not used."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner (use with 'repo' as the bare repository name)."
          },
          "repository": {
            "type": "string",
            "description": "Repository in 'owner/repo' format (alias of 'repo')."
          },
          "url": {
            "type": "string",
            "description": "GitHub repository URL to extract owner/repo from."
          },
          "latest": {
            "type": "boolean",
            "description": "Return only the single highest-version release per repo. Set to false to return the full filtered, version-sorted list.",
            "default": true
          },
          "constraint": {
            "type": "string",
            "description": "Restrict to a version line by prefix: a major ('4' or 'v4') or major.minor ('4.2' or 'v4.2'). The highest matching version is returned. Range operators (^, ~, >=, <) are not supported."
          },
          "include_prereleases": {
            "type": "boolean",
            "description": "Consider prereleases. By default they are excluded, matching GitHub's releases/latest behavior.",
            "default": false
          },
          "include_drafts": {
            "type": "boolean",
            "description": "Consider draft releases (only visible with appropriate authorization). Excluded by default.",
            "default": false
          },
          "limit": {
            "type": "integer",
            "description": "Number of releases fetched per repo before filtering and sorting. Capped at 100.",
            "default": 30,
            "minimum": 1
          },
          "resolve_sha": {
            "type": "boolean",
            "description": "Resolve each returned tag to its commit SHA (adds a 'commit_sha' field). Useful for pinning GitHub Actions to an immutable SHA. Costs one extra API call per returned release.",
            "default": false
          },
          "fields": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "repo",
                "tag_name",
                "name",
                "published_at",
                "html_url",
                "prerelease",
                "draft",
                "commit_sha"
              ]
            },
            "description": "Restrict each result object to these fields. Omit for the full object. Example: ['tag_name'] returns only version numbers."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression applied to the final result array. Syntax is validated before execution. Output is JSON; e.g. '.[].tag_name' reduces it to the (JSON-quoted) version strings."
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr. In batch mode, a repo whose lookup fails is skipped instead of failing the whole call.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if a release lookup fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "api_read",
      "description": "Execute a read-only GitHub API call using gh api. Restricted to GET requests only. Use the gh-tooling-write server's api tool for POST, PATCH, PUT, or DELETE requests. Use this as an escape hatch when specific read tools don't cover your use case.",
      "inputSchema": {
        "type": "object",
        "required": [
          "endpoint"
        ],
        "properties": {
          "endpoint": {
            "type": "string",
            "description": "API endpoint path relative to https://api.github.com/ (e.g. 'repos/shopware/shopware/pulls/14642/comments')."
          },
          "method": {
            "type": "string",
            "enum": [
              "GET",
              "POST",
              "PATCH",
              "PUT",
              "DELETE"
            ],
            "description": "HTTP method.",
            "default": "GET"
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the JSON response. Syntax is validated before execution."
          },
          "paginate": {
            "type": "boolean",
            "description": "Fetch all pages of paginated results.",
            "default": false
          },
          "fields": {
            "type": "string",
            "description": "Comma-separated fields for --jq '{field1, field2}' selection. Use 'jq_filter' for complex expressions."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output (head -n N). Useful for large API responses.",
            "minimum": 1
          },
          "tail_lines": {
            "type": "integer",
            "description": "Return only the last N lines of output. Applied after max_lines if both are set.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output. When true, gh errors are silenced; the tool returns empty output instead of an error message. Useful when the resource may not exist.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails (non-zero exit). When provided, the tool always succeeds and the fallback text is returned instead of the error."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "label_list",
      "description": "List labels for a repository. Returns label names, descriptions, and colors.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format."
          },
          "filter": {
            "type": "string",
            "description": "Filter labels by name substring (case-insensitive)."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the JSON output."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "project_list",
      "description": "List GitHub Projects (v2) for a user or organization. Returns project numbers, titles, and URLs.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "owner": {
            "type": "string",
            "description": "Owner (user or org) to list projects for. Defaults to the owner from the configured default repo."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the JSON output."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "project_view",
      "description": "View details of a GitHub Project (v2), including field definitions and status options. Use to discover available status values before setting them.",
      "inputSchema": {
        "type": "object",
        "required": [
          "number"
        ],
        "properties": {
          "number": {
            "type": ["integer", "string"],
            "description": "Project number."
          },
          "owner": {
            "type": "string",
            "description": "Owner (user or org). Defaults to the owner from the configured default repo."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the JSON output."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "issue_schema",
      "description": "List the issue types and issue fields available for an organization, including each single-select field's options. Use before setting an issue type or a field value to discover the exact names the write tools expect. Types and fields are independent: any organization field can be set on an issue of any type.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "org": {
            "type": "string",
            "description": "Organization login. Defaults to the owner of the resolved repository."
          },
          "owner": {
            "type": "string",
            "description": "Repository owner, used as the organization when 'org' is not set."
          },
          "repo": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format, used to derive the organization."
          },
          "repository": {
            "type": "string",
            "description": "GitHub repository in 'owner/repo' format. Alias of `repo`."
          },
          "type": {
            "type": "string",
            "description": "Return only the issue type with this exact name (case-insensitive). Errors when no type matches."
          },
          "field": {
            "type": "string",
            "description": "Return only the issue field with this exact name (case-insensitive). Errors when no field matches."
          },
          "jq_filter": {
            "type": "string",
            "description": "jq expression to filter/transform the JSON output."
          },
          "max_lines": {
            "type": "integer",
            "description": "Return only the first N lines of output.",
            "minimum": 1
          },
          "suppress_errors": {
            "type": "boolean",
            "description": "Discard stderr output.",
            "default": false
          },
          "fallback": {
            "type": "string",
            "description": "Text to return if the gh command fails."
          }
        },
        "additionalProperties": false
      }
    }
  ]
}
